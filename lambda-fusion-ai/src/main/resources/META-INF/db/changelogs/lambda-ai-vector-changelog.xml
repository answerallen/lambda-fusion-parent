<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- ========================================== -->
    <!-- PostgresSQL扩展创建 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-2026020300011" author="Jin">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sql>
            CREATE EXTENSION IF NOT EXISTS vector;
            CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
            CREATE EXTENSION IF NOT EXISTS pg_trgm;
        </sql>
        <rollback>
            DROP EXTENSION IF EXISTS pg_trgm;
            DROP EXTENSION IF EXISTS "uuid-ossp";
            DROP EXTENSION IF EXISTS vector;
        </rollback>
    </changeSet>

    <!-- ========================================== -->
    <!-- 向量子表动态创建存储过程 -->
    <!-- runOnChange: 函数体演进时重新应用 CREATE OR REPLACE，避免校验和变更失败 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-20260205001001" author="Jin" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <createProcedure>
            CREATE OR REPLACE FUNCTION create_vector_table(dimension INTEGER)
            RETURNS TEXT AS $$
            DECLARE
                v_table_name TEXT;
            BEGIN
                v_table_name := 'ai_vector_store_' || dimension::TEXT;
                IF EXISTS (SELECT 1 FROM information_schema.tables t WHERE t.table_name = v_table_name) THEN
                    RETURN v_table_name || ' already exists';
                END IF;

                IF dimension &lt;= 2000 THEN
                    EXECUTE format('
                        CREATE TABLE %I (
                            id BIGSERIAL PRIMARY KEY,
                            vector_id VARCHAR(100) UNIQUE NOT NULL,
                            kb_id VARCHAR(64) NOT NULL,
                            tenant_id VARCHAR(32) NOT NULL,
                            document_id VARCHAR(64),
                            chunk_id VARCHAR(64),
                            collection_name VARCHAR(100) NOT NULL,
                            embedding vector(%s) NOT NULL,
                            content TEXT NOT NULL,
                            metadata JSONB,
                            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                            FOREIGN KEY (kb_id) REFERENCES ai_knowledge_base(id) ON DELETE CASCADE,
                            FOREIGN KEY (document_id) REFERENCES ai_document(id) ON DELETE CASCADE,
                            FOREIGN KEY (chunk_id) REFERENCES ai_document_chunk(id) ON DELETE CASCADE
                        );
                        CREATE INDEX idx_%I_kb_id ON %I (kb_id);
                        CREATE INDEX idx_%I_tenant_id ON %I (tenant_id);
                        CREATE INDEX idx_%I_embedding ON %I USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
                        CREATE INDEX idx_%I_content_trgm ON %I USING gist (content gist_trgm_ops);
                    ', v_table_name, dimension, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name);
                ELSIF dimension &lt;= 4000 THEN
                    EXECUTE format('
                        CREATE TABLE %I (
                            id BIGSERIAL PRIMARY KEY,
                            vector_id VARCHAR(100) UNIQUE NOT NULL,
                            kb_id VARCHAR(64) NOT NULL,
                            tenant_id VARCHAR(32) NOT NULL,
                            document_id VARCHAR(64),
                            chunk_id VARCHAR(64),
                            collection_name VARCHAR(100) NOT NULL,
                            embedding halfvec(%s) NOT NULL,
                            content TEXT NOT NULL,
                            metadata JSONB,
                            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                            FOREIGN KEY (kb_id) REFERENCES ai_knowledge_base(id) ON DELETE CASCADE,
                            FOREIGN KEY (document_id) REFERENCES ai_document(id) ON DELETE CASCADE,
                            FOREIGN KEY (chunk_id) REFERENCES ai_document_chunk(id) ON DELETE CASCADE
                        );
                        CREATE INDEX idx_%I_kb_id ON %I (kb_id);
                        CREATE INDEX idx_%I_tenant_id ON %I (tenant_id);
                        CREATE INDEX idx_%I_embedding ON %I USING hnsw (embedding halfvec_cosine_ops);
                        CREATE INDEX idx_%I_content_trgm ON %I USING gist (content gist_trgm_ops);
                    ', v_table_name, dimension, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name);
                ELSE
                    EXECUTE format('
                        CREATE TABLE %I (
                            id BIGSERIAL PRIMARY KEY,
                            vector_id VARCHAR(100) UNIQUE NOT NULL,
                            kb_id VARCHAR(64) NOT NULL,
                            tenant_id VARCHAR(32) NOT NULL,
                            document_id VARCHAR(64),
                            chunk_id VARCHAR(64),
                            collection_name VARCHAR(100) NOT NULL,
                            embedding vector(%s) NOT NULL,
                            content TEXT NOT NULL,
                            metadata JSONB,
                            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                            FOREIGN KEY (kb_id) REFERENCES ai_knowledge_base(id) ON DELETE CASCADE,
                            FOREIGN KEY (document_id) REFERENCES ai_document(id) ON DELETE CASCADE,
                            FOREIGN KEY (chunk_id) REFERENCES ai_document_chunk(id) ON DELETE CASCADE
                        );
                        CREATE INDEX idx_%I_kb_id ON %I (kb_id);
                        CREATE INDEX idx_%I_tenant_id ON %I (tenant_id);
                        CREATE INDEX idx_%I_content_trgm ON %I USING gist (content gist_trgm_ops);
                    ', v_table_name, dimension, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name, v_table_name);
                END IF;
                RETURN v_table_name || ' created successfully';
            END;
            $$ LANGUAGE plpgsql;
        </createProcedure>
        <sql>
            SELECT create_vector_table(768);
            SELECT create_vector_table(1536);
            SELECT create_vector_table(2048);
            SELECT create_vector_table(4096);
        </sql>
    </changeSet>

    <!-- ========================================== -->
    <!-- 向量表补充 tenant_id（存量 768/1536/2048/4096 分表） -->
    <!-- create_vector_table 对已存在表 early-return，故存量表需显式 ALTER 回填 -->
    <!-- tenant_id 对单 KB 检索与 kb_id 冗余，但提供防御性作用域与跨租户安全过滤 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-20260715001001" author="zx">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sql>
            ALTER TABLE ai_vector_store_768 ADD COLUMN IF NOT EXISTS tenant_id VARCHAR(32);
            ALTER TABLE ai_vector_store_1536 ADD COLUMN IF NOT EXISTS tenant_id VARCHAR(32);
            ALTER TABLE ai_vector_store_2048 ADD COLUMN IF NOT EXISTS tenant_id VARCHAR(32);
            ALTER TABLE ai_vector_store_4096 ADD COLUMN IF NOT EXISTS tenant_id VARCHAR(32);

            UPDATE ai_vector_store_768 v SET tenant_id = kb.tenant_id
            FROM ai_knowledge_base kb WHERE v.kb_id = kb.id AND v.tenant_id IS NULL;
            UPDATE ai_vector_store_1536 v SET tenant_id = kb.tenant_id
            FROM ai_knowledge_base kb WHERE v.kb_id = kb.id AND v.tenant_id IS NULL;
            UPDATE ai_vector_store_2048 v SET tenant_id = kb.tenant_id
            FROM ai_knowledge_base kb WHERE v.kb_id = kb.id AND v.tenant_id IS NULL;
            UPDATE ai_vector_store_4096 v SET tenant_id = kb.tenant_id
            FROM ai_knowledge_base kb WHERE v.kb_id = kb.id AND v.tenant_id IS NULL;

            CREATE INDEX IF NOT EXISTS idx_ai_vector_store_768_tenant_id ON ai_vector_store_768 (tenant_id);
            CREATE INDEX IF NOT EXISTS idx_ai_vector_store_1536_tenant_id ON ai_vector_store_1536 (tenant_id);
            CREATE INDEX IF NOT EXISTS idx_ai_vector_store_2048_tenant_id ON ai_vector_store_2048 (tenant_id);
            CREATE INDEX IF NOT EXISTS idx_ai_vector_store_4096_tenant_id ON ai_vector_store_4096 (tenant_id);
        </sql>
    </changeSet>

</databaseChangeLog>
