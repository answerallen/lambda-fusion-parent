<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- ========================================== -->
    <!-- PostgresSQL扩展创建 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-202602030001" author="Jin">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sql>
            CREATE EXTENSION IF NOT EXISTS vector;
            CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
            CREATE EXTENSION IF NOT EXISTS pg_trgm;
        </sql>
        <rollback>
            DROP EXTENSION IF EXISTS pg_trgm;
            DROP EXTENSION IF EXISTS "uuid-ossp";
            DROP EXTENSION IF EXISTS vector;
        </rollback>
    </changeSet>

    <!-- ========================================== -->
    <!-- 向量子表动态创建存储过程 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-202602050010" author="Jin">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <createProcedure>
            CREATE OR REPLACE FUNCTION create_vector_table(dimension INTEGER)
            RETURNS TEXT AS $$
            DECLARE
                table_name TEXT;
            BEGIN
                table_name := 'ai_vector_store_' || dimension::TEXT;
                IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = table_name) THEN
                    RETURN table_name || ' already exists';
                END IF;
                EXECUTE format('
                    CREATE TABLE %I (
                        id BIGSERIAL PRIMARY KEY,
                        vector_id VARCHAR(100) UNIQUE NOT NULL,
                        kb_id BIGINT NOT NULL,
                        document_id BIGINT,
                        chunk_id BIGINT,
                        collection_name VARCHAR(100) NOT NULL,
                        embedding vector(%s) NOT NULL,
                        content TEXT NOT NULL,
                        metadata JSONB,
                        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                        FOREIGN KEY (kb_id) REFERENCES ai_knowledge_base(id) ON DELETE CASCADE,
                        FOREIGN KEY (document_id) REFERENCES ai_document(id) ON DELETE CASCADE,
                        FOREIGN KEY (chunk_id) REFERENCES ai_document_chunk(id) ON DELETE CASCADE
                    );
                    CREATE INDEX idx_%I_kb_id ON %I (kb_id);
                    CREATE INDEX idx_%I_embedding ON %I USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
                    CREATE INDEX idx_%I_content_trgm ON %I USING gist (content gist_trgm_ops);
                ', table_name, dimension, 
                   table_name, table_name,
                   table_name, table_name,
                   table_name, table_name);
                RETURN table_name || ' created successfully';
            END;
            $$ LANGUAGE plpgsql;
        </createProcedure>
        <sql>
            SELECT create_vector_table(768);
            SELECT create_vector_table(1536);
        </sql>
    </changeSet>

    <!-- ========================================== -->
    <!-- 统一向量表 ai_vector_store -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-202602060010" author="zx">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
            <not>
                <tableExists tableName="ai_vector_store"/>
            </not>
        </preConditions>
        <createTable tableName="ai_vector_store">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>
            <column name="vector_id" type="varchar(100)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="kb_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="document_id" type="bigint"/>
            <column name="chunk_id" type="bigint"/>
            <column name="embedding" type="vector(1536)">
                <constraints nullable="false"/>
            </column>
            <column name="dimension" type="integer" defaultValue="1536">
                <constraints nullable="false"/>
            </column>
            <column name="content" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="metadata" type="jsonb"/>
            <column name="created_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP"/>
        </createTable>
        <createIndex tableName="ai_vector_store" indexName="idx_vector_kb_id">
            <column name="kb_id"/>
        </createIndex>
        <createIndex tableName="ai_vector_store" indexName="idx_vector_dimension">
            <column name="dimension"/>
        </createIndex>
        <sql>
            CREATE INDEX idx_vector_embedding ON ai_vector_store
                USING hnsw (embedding vector_cosine_ops)
                WITH (m = 16, ef_construction = 64);
            CREATE INDEX idx_vector_content_trgm ON ai_vector_store
                USING gist (content gist_trgm_ops);
        </sql>
    </changeSet>

    <!-- ========================================== -->
    <!-- 向量表多维度支持 -->
    <!-- ========================================== -->
    <changeSet id="lambda-fusion-ai-202604010010" author="Jin">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
            <tableExists tableName="ai_vector_store"/>
        </preConditions>
        <sql>
            DROP INDEX IF EXISTS idx_vector_embedding;
            ALTER TABLE ai_vector_store 
            ALTER COLUMN embedding TYPE vector(4096);
            CREATE INDEX idx_vector_embedding ON ai_vector_store
                USING hnsw (embedding vector_cosine_ops)
                WITH (m = 16, ef_construction = 64);
        </sql>
        <rollback>
            <sql>
                DROP INDEX IF EXISTS idx_vector_embedding;
                ALTER TABLE ai_vector_store 
                ALTER COLUMN embedding TYPE vector(1536);
                CREATE INDEX idx_vector_embedding ON ai_vector_store
                    USING hnsw (embedding vector_cosine_ops)
                    WITH (m = 16, ef_construction = 64);
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
